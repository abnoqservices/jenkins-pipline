diff --git a/node_modules/@measured/puck/dist/chunk-QIGVND56.mjs b/node_modules/@measured/puck/dist/chunk-QIGVND56.mjs
index ee1d987..2956a0a 100644
--- a/node_modules/@measured/puck/dist/chunk-QIGVND56.mjs
+++ b/node_modules/@measured/puck/dist/chunk-QIGVND56.mjs
@@ -5097,12 +5097,14 @@ var DragDropContextClient = ({
             const [sourceId] = source.id.split(":");
             const [targetId] = target.id.split(":");
             const sourceData = source.data;
+            if (!sourceData) return;
             let sourceZone = sourceData.zone;
             let sourceIndex = sourceData.index;
             let targetZone = "";
             let targetIndex = 0;
             if (target.type === "component") {
               const targetData = target.data;
+              if (!targetData) return;
               targetZone = targetData.zone;
               targetIndex = targetData.index;
               const collisionData = (_b = manager.collisionObserver.collisions[0]) == null ? void 0 : _b.data;
@@ -5175,6 +5177,7 @@ var DragDropContextClient = ({
             const { source } = event.operation;
             if (source && source.type !== "void") {
               const sourceData = source.data;
+              if (sourceData) {
               const item = getItem(
                 {
                   zone: sourceData.zone,
@@ -5196,6 +5199,7 @@ var DragDropContextClient = ({
                   }
                 });
               }
+              }
             }
             (_a = dragListeners.dragstart) == null ? void 0 : _a.forEach((fn) => {
               fn(event, manager);
@@ -5492,20 +5496,24 @@ var useContentIdsWithPreview = (contentIds, zoneCompound) => {
         return;
       }
       if (preview2) {
-        if (preview2.type === "insert") {
+        const previewId = preview2.props == null ? void 0 : preview2.props.id;
+        if (previewId == null || previewId === "") {
+          setContentIdsWithPreview(contentIds2);
+          setLocalPreview(void 0);
+        } else if (preview2.type === "insert") {
           setContentIdsWithPreview(
             insert(
-              contentIds2.filter((id) => id !== preview2.props.id),
+              contentIds2.filter((id) => id !== previewId),
               preview2.index,
-              preview2.props.id
+              previewId
             )
           );
         } else {
           setContentIdsWithPreview(
             insert(
-              contentIds2.filter((id) => id !== preview2.props.id),
+              contentIds2.filter((id) => id !== previewId),
               preview2.index,
-              preview2.props.id
+              previewId
             )
           );
         }
@@ -5900,10 +5908,11 @@ var DropZoneChild = ({
       return expanded;
     }
     const preview = zoneStore.getState().previewIndex[zoneCompound];
-    if (componentId === (preview == null ? void 0 : preview.props.id)) {
+    const previewId = preview == null ? void 0 : preview.props == null ? void 0 : preview.props.id;
+    if (componentId === previewId && preview && preview.componentType) {
       return {
         type: preview.componentType,
-        props: preview.props,
+        props: preview.props || {},
         previewType: preview.type,
         element: preview.element
       };
@@ -6108,8 +6117,8 @@ var DropZoneEdit = forwardRef3(
       [allow, disallow]
     );
     const targetAccepted = useContextStore(ZoneStoreContext, (s) => {
-      var _a;
-      const draggedComponentType = (_a = s.draggedItem) == null ? void 0 : _a.data.componentType;
+      var _a, _b;
+      const draggedComponentType = (_b = (_a = s.draggedItem) == null ? void 0 : _a.data) == null ? void 0 : _b.componentType;
       return acceptsTarget(draggedComponentType);
     });
     const hoveringOverArea = inNextDeepestArea || isRootZone;
@@ -6189,6 +6198,7 @@ var DropZoneEdit = forwardRef3(
           backgroundColor: RENDER_DEBUG ? getRandomColor() : style == null ? void 0 : style.backgroundColor
         }),
         children: contentIdsWithPreview.map((componentId, i) => {
+          const dropChildKey = `${zoneCompound}:${i}:${componentId != null && componentId !== "" ? componentId : "nil"}`;
           return /* @__PURE__ */ jsx26(
             DropZoneChildMemo,
             {
@@ -6199,7 +6209,7 @@ var DropZoneEdit = forwardRef3(
               collisionAxis,
               inDroppableZone: targetAccepted
             },
-            componentId
+            dropChildKey
           );
         })
       }
@@ -7344,6 +7354,7 @@ var Layer = ({
   });
   const isSelected = selecedItemId === itemId || itemSelector && itemSelector.zone === rootDroppableId && !zoneCompound;
   const nodeData = useAppStore((s) => s.state.indexes.nodes[itemId]);
+  if (!nodeData) return null;
   const zonesForItem = useAppStore(
     useShallow6(
       (s) => Object.keys(s.state.indexes.zones).filter(
@@ -7415,22 +7426,24 @@ var Layer = ({
               zoneStore.setState({ hoveringComponent: null });
             },
             children: [
-              containsZone && /* @__PURE__ */ jsx35(
+              containsZone ? /* @__PURE__ */ jsx35(
                 "div",
                 {
                   className: getClassNameLayer("chevron"),
                   title: isSelected ? "Collapse" : "Expand",
                   children: /* @__PURE__ */ jsx35(ChevronDown, { size: "12" })
-                }
-              ),
+                },
+                "puck-chevron"
+              ) : null,
               /* @__PURE__ */ jsxs15("div", { className: getClassNameLayer("title"), children: [
-                /* @__PURE__ */ jsx35("div", { className: getClassNameLayer("icon"), children: nodeData.data.type === "Text" || nodeData.data.type === "Heading" ? /* @__PURE__ */ jsx35(Type, { size: "16" }) : /* @__PURE__ */ jsx35(LayoutGrid, { size: "16" }) }),
-                /* @__PURE__ */ jsx35("div", { className: getClassNameLayer("name"), children: label })
-              ] })
+                /* @__PURE__ */ jsx35("div", { className: getClassNameLayer("icon"), children: nodeData.data.type === "Text" || nodeData.data.type === "Heading" ? /* @__PURE__ */ jsx35(Type, { size: "16" }) : /* @__PURE__ */ jsx35(LayoutGrid, { size: "16" }) }, "puck-icon"),
+                /* @__PURE__ */ jsx35("div", { className: getClassNameLayer("name"), children: label }, "puck-name")
+              ] }, "puck-title")
             ]
           }
-        ) }),
-        containsZone && zonesForItem.map((subzone) => /* @__PURE__ */ jsx35("div", { className: getClassNameLayer("zones"), children: /* @__PURE__ */ jsx35(LayerTree, { zoneCompound: subzone }) }, subzone))
+        )
+        }, `puck-layer-row-${itemId}`),
+        containsZone ? zonesForItem.map((subzone) => /* @__PURE__ */ jsx35("div", { className: getClassNameLayer("zones"), children: /* @__PURE__ */ jsx35(LayerTree, { zoneCompound: subzone }) }, subzone)) : null
       ]
     }
   );
@@ -7457,24 +7470,22 @@ var LayerTree = ({
     )
   );
   return /* @__PURE__ */ jsxs15(Fragment11, { children: [
-    label && /* @__PURE__ */ jsxs15("div", { className: getClassName24("zoneTitle"), children: [
-      /* @__PURE__ */ jsx35("div", { className: getClassName24("zoneIcon"), children: /* @__PURE__ */ jsx35(Layers, { size: "16" }) }),
+    label ? /* @__PURE__ */ jsxs15("div", { className: getClassName24("zoneTitle"), children: [
+      /* @__PURE__ */ jsx35("div", { className: getClassName24("zoneIcon"), children: /* @__PURE__ */ jsx35(Layers, { size: "16" }) }, "puck-zone-icon"),
       label
-    ] }),
-    /* @__PURE__ */ jsxs15("ul", { className: getClassName24(), children: [
-      contentIds.length === 0 && /* @__PURE__ */ jsx35("div", { className: getClassName24("helper"), children: "No items" }),
-      contentIds.map((itemId, i) => {
-        return /* @__PURE__ */ jsx35(
-          Layer,
-          {
-            index: i,
-            itemId,
-            zoneCompound
-          },
-          itemId
-        );
-      })
-    ] })
+    ] }, "puck-zone-heading") : null,
+    /* @__PURE__ */ jsxs15("ul", { className: getClassName24(), children: contentIds.length === 0 ? /* @__PURE__ */ jsx35("div", { className: getClassName24("helper"), children: "No items" }, "puck-layer-empty") : contentIds.map((itemId, i) => {
+      const layerKey = `${zoneCompound != null ? zoneCompound : "root"}:${i}:${itemId != null && itemId !== "" ? itemId : "missing"}`;
+      return /* @__PURE__ */ jsx35(
+        Layer,
+        {
+          index: i,
+          itemId,
+          zoneCompound
+        },
+        layerKey
+      );
+    }) }, "puck-zone-ul")
   ] });
 };
 
diff --git a/node_modules/@measured/puck/dist/index.js b/node_modules/@measured/puck/dist/index.js
index 4bbeac5..5356727 100644
--- a/node_modules/@measured/puck/dist/index.js
+++ b/node_modules/@measured/puck/dist/index.js
@@ -5227,20 +5227,24 @@ var useContentIdsWithPreview = (contentIds, zoneCompound) => {
         return;
       }
       if (preview2) {
-        if (preview2.type === "insert") {
+        const previewId = preview2.props == null ? void 0 : preview2.props.id;
+        if (previewId == null || previewId === "") {
+          setContentIdsWithPreview(contentIds2);
+          setLocalPreview(void 0);
+        } else if (preview2.type === "insert") {
           setContentIdsWithPreview(
             insert(
-              contentIds2.filter((id) => id !== preview2.props.id),
+              contentIds2.filter((id) => id !== previewId),
               preview2.index,
-              preview2.props.id
+              previewId
             )
           );
         } else {
           setContentIdsWithPreview(
             insert(
-              contentIds2.filter((id) => id !== preview2.props.id),
+              contentIds2.filter((id) => id !== previewId),
               preview2.index,
-              preview2.props.id
+              previewId
             )
           );
         }
@@ -5797,10 +5801,11 @@ var DropZoneChild = ({
       return expanded;
     }
     const preview = zoneStore.getState().previewIndex[zoneCompound];
-    if (componentId === (preview == null ? void 0 : preview.props.id)) {
+    const previewId = preview == null ? void 0 : preview.props == null ? void 0 : preview.props.id;
+    if (componentId === previewId && preview && preview.componentType) {
       return {
         type: preview.componentType,
-        props: preview.props,
+        props: preview.props || {},
         previewType: preview.type,
         element: preview.element
       };
@@ -6005,8 +6010,8 @@ var DropZoneEdit = (0, import_react34.forwardRef)(
       [allow, disallow]
     );
     const targetAccepted = useContextStore(ZoneStoreContext, (s) => {
-      var _a;
-      const draggedComponentType = (_a = s.draggedItem) == null ? void 0 : _a.data.componentType;
+      var _a, _b;
+      const draggedComponentType = (_b = (_a = s.draggedItem) == null ? void 0 : _a.data) == null ? void 0 : _b.componentType;
       return acceptsTarget(draggedComponentType);
     });
     const hoveringOverArea = inNextDeepestArea || isRootZone;
@@ -6086,6 +6091,7 @@ var DropZoneEdit = (0, import_react34.forwardRef)(
           backgroundColor: RENDER_DEBUG ? getRandomColor() : style == null ? void 0 : style.backgroundColor
         }),
         children: contentIdsWithPreview.map((componentId, i) => {
+          const dropChildKey = `${zoneCompound}:${i}:${componentId != null && componentId !== "" ? componentId : "nil"}`;
           return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
             DropZoneChildMemo,
             {
@@ -6096,7 +6102,7 @@ var DropZoneEdit = (0, import_react34.forwardRef)(
               collisionAxis,
               inDroppableZone: targetAccepted
             },
-            componentId
+            dropChildKey
           );
         })
       }
@@ -6769,12 +6775,14 @@ var DragDropContextClient = ({
             const [sourceId] = source.id.split(":");
             const [targetId] = target.id.split(":");
             const sourceData = source.data;
+            if (!sourceData) return;
             let sourceZone = sourceData.zone;
             let sourceIndex = sourceData.index;
             let targetZone = "";
             let targetIndex = 0;
             if (target.type === "component") {
               const targetData = target.data;
+              if (!targetData) return;
               targetZone = targetData.zone;
               targetIndex = targetData.index;
               const collisionData = (_b = manager.collisionObserver.collisions[0]) == null ? void 0 : _b.data;
@@ -6847,6 +6855,7 @@ var DragDropContextClient = ({
             const { source } = event.operation;
             if (source && source.type !== "void") {
               const sourceData = source.data;
+              if (sourceData) {
               const item = getItem(
                 {
                   zone: sourceData.zone,
@@ -6868,6 +6877,7 @@ var DragDropContextClient = ({
                   }
                 });
               }
+              }
             }
             (_a = dragListeners.dragstart) == null ? void 0 : _a.forEach((fn) => {
               fn(event, manager);
@@ -7960,6 +7970,7 @@ var Layer = ({
   });
   const isSelected = selecedItemId === itemId || itemSelector && itemSelector.zone === rootDroppableId && !zoneCompound;
   const nodeData = useAppStore((s) => s.state.indexes.nodes[itemId]);
+  if (!nodeData) return null;
   const zonesForItem = useAppStore(
     (0, import_shallow6.useShallow)(
       (s) => Object.keys(s.state.indexes.zones).filter(
@@ -8031,22 +8042,24 @@ var Layer = ({
               zoneStore.setState({ hoveringComponent: null });
             },
             children: [
-              containsZone && /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
+              containsZone ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
                 "div",
                 {
                   className: getClassNameLayer("chevron"),
                   title: isSelected ? "Collapse" : "Expand",
                   children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ChevronDown, { size: "12" })
-                }
-              ),
+                },
+                "puck-chevron"
+              ) : null,
               /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("div", { className: getClassNameLayer("title"), children: [
-                /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("icon"), children: nodeData.data.type === "Text" || nodeData.data.type === "Heading" ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(Type, { size: "16" }) : /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(LayoutGrid, { size: "16" }) }),
-                /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("name"), children: label })
-              ] })
+                /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("icon"), children: nodeData.data.type === "Text" || nodeData.data.type === "Heading" ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(Type, { size: "16" }) : /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(LayoutGrid, { size: "16" }) }, "puck-icon"),
+                /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("name"), children: label }, "puck-name")
+              ] }, "puck-title")
             ]
           }
-        ) }),
-        containsZone && zonesForItem.map((subzone) => /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("zones"), children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(LayerTree, { zoneCompound: subzone }) }, subzone))
+        )
+        }, `puck-layer-row-${itemId}`),
+        containsZone ? zonesForItem.map((subzone) => /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("zones"), children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(LayerTree, { zoneCompound: subzone }) }, subzone)) : null
       ]
     }
   );
@@ -8073,24 +8086,22 @@ var LayerTree = ({
     )
   );
   return /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)(import_jsx_runtime36.Fragment, { children: [
-    label && /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("div", { className: getClassName24("zoneTitle"), children: [
-      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassName24("zoneIcon"), children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(Layers, { size: "16" }) }),
+    label ? /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("div", { className: getClassName24("zoneTitle"), children: [
+      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassName24("zoneIcon"), children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(Layers, { size: "16" }) }, "puck-zone-icon"),
       label
-    ] }),
-    /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("ul", { className: getClassName24(), children: [
-      contentIds.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassName24("helper"), children: "No items" }),
-      contentIds.map((itemId, i) => {
-        return /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
-          Layer,
-          {
-            index: i,
-            itemId,
-            zoneCompound
-          },
-          itemId
-        );
-      })
-    ] })
+    ] }, "puck-zone-heading") : null,
+    /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("ul", { className: getClassName24(), children: contentIds.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassName24("helper"), children: "No items" }, "puck-layer-empty") : contentIds.map((itemId, i) => {
+      const layerKey = `${zoneCompound != null ? zoneCompound : "root"}:${i}:${itemId != null && itemId !== "" ? itemId : "missing"}`;
+      return /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
+        Layer,
+        {
+          index: i,
+          itemId,
+          zoneCompound
+        },
+        layerKey
+      );
+    }) }, "puck-zone-ul")
   ] });
 };
 
diff --git a/node_modules/@measured/puck/dist/no-external.js b/node_modules/@measured/puck/dist/no-external.js
index 488cc17..eb63d0b 100644
--- a/node_modules/@measured/puck/dist/no-external.js
+++ b/node_modules/@measured/puck/dist/no-external.js
@@ -5227,20 +5227,24 @@ var useContentIdsWithPreview = (contentIds, zoneCompound) => {
         return;
       }
       if (preview2) {
-        if (preview2.type === "insert") {
+        const previewId = preview2.props == null ? void 0 : preview2.props.id;
+        if (previewId == null || previewId === "") {
+          setContentIdsWithPreview(contentIds2);
+          setLocalPreview(void 0);
+        } else if (preview2.type === "insert") {
           setContentIdsWithPreview(
             insert(
-              contentIds2.filter((id) => id !== preview2.props.id),
+              contentIds2.filter((id) => id !== previewId),
               preview2.index,
-              preview2.props.id
+              previewId
             )
           );
         } else {
           setContentIdsWithPreview(
             insert(
-              contentIds2.filter((id) => id !== preview2.props.id),
+              contentIds2.filter((id) => id !== previewId),
               preview2.index,
-              preview2.props.id
+              previewId
             )
           );
         }
@@ -5797,10 +5801,11 @@ var DropZoneChild = ({
       return expanded;
     }
     const preview = zoneStore.getState().previewIndex[zoneCompound];
-    if (componentId === (preview == null ? void 0 : preview.props.id)) {
+    const previewId = preview == null ? void 0 : preview.props == null ? void 0 : preview.props.id;
+    if (componentId === previewId && preview && preview.componentType) {
       return {
         type: preview.componentType,
-        props: preview.props,
+        props: preview.props || {},
         previewType: preview.type,
         element: preview.element
       };
@@ -6005,8 +6010,8 @@ var DropZoneEdit = (0, import_react34.forwardRef)(
       [allow, disallow]
     );
     const targetAccepted = useContextStore(ZoneStoreContext, (s) => {
-      var _a;
-      const draggedComponentType = (_a = s.draggedItem) == null ? void 0 : _a.data.componentType;
+      var _a, _b;
+      const draggedComponentType = (_b = (_a = s.draggedItem) == null ? void 0 : _a.data) == null ? void 0 : _b.componentType;
       return acceptsTarget(draggedComponentType);
     });
     const hoveringOverArea = inNextDeepestArea || isRootZone;
@@ -6086,6 +6091,7 @@ var DropZoneEdit = (0, import_react34.forwardRef)(
           backgroundColor: RENDER_DEBUG ? getRandomColor() : style == null ? void 0 : style.backgroundColor
         }),
         children: contentIdsWithPreview.map((componentId, i) => {
+          const dropChildKey = `${zoneCompound}:${i}:${componentId != null && componentId !== "" ? componentId : "nil"}`;
           return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
             DropZoneChildMemo,
             {
@@ -6096,7 +6102,7 @@ var DropZoneEdit = (0, import_react34.forwardRef)(
               collisionAxis,
               inDroppableZone: targetAccepted
             },
-            componentId
+            dropChildKey
           );
         })
       }
@@ -6769,12 +6775,14 @@ var DragDropContextClient = ({
             const [sourceId] = source.id.split(":");
             const [targetId] = target.id.split(":");
             const sourceData = source.data;
+            if (!sourceData) return;
             let sourceZone = sourceData.zone;
             let sourceIndex = sourceData.index;
             let targetZone = "";
             let targetIndex = 0;
             if (target.type === "component") {
               const targetData = target.data;
+              if (!targetData) return;
               targetZone = targetData.zone;
               targetIndex = targetData.index;
               const collisionData = (_b = manager.collisionObserver.collisions[0]) == null ? void 0 : _b.data;
@@ -6847,6 +6855,7 @@ var DragDropContextClient = ({
             const { source } = event.operation;
             if (source && source.type !== "void") {
               const sourceData = source.data;
+              if (sourceData) {
               const item = getItem(
                 {
                   zone: sourceData.zone,
@@ -6868,6 +6877,7 @@ var DragDropContextClient = ({
                   }
                 });
               }
+              }
             }
             (_a = dragListeners.dragstart) == null ? void 0 : _a.forEach((fn) => {
               fn(event, manager);
@@ -7960,6 +7970,7 @@ var Layer = ({
   });
   const isSelected = selecedItemId === itemId || itemSelector && itemSelector.zone === rootDroppableId && !zoneCompound;
   const nodeData = useAppStore((s) => s.state.indexes.nodes[itemId]);
+  if (!nodeData) return null;
   const zonesForItem = useAppStore(
     (0, import_shallow6.useShallow)(
       (s) => Object.keys(s.state.indexes.zones).filter(
@@ -8031,22 +8042,24 @@ var Layer = ({
               zoneStore.setState({ hoveringComponent: null });
             },
             children: [
-              containsZone && /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
+              containsZone ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
                 "div",
                 {
                   className: getClassNameLayer("chevron"),
                   title: isSelected ? "Collapse" : "Expand",
                   children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ChevronDown, { size: "12" })
-                }
-              ),
+                },
+                "puck-chevron"
+              ) : null,
               /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("div", { className: getClassNameLayer("title"), children: [
-                /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("icon"), children: nodeData.data.type === "Text" || nodeData.data.type === "Heading" ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(Type, { size: "16" }) : /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(LayoutGrid, { size: "16" }) }),
-                /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("name"), children: label })
-              ] })
+                /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("icon"), children: nodeData.data.type === "Text" || nodeData.data.type === "Heading" ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(Type, { size: "16" }) : /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(LayoutGrid, { size: "16" }) }, "puck-icon"),
+                /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("name"), children: label }, "puck-name")
+              ] }, "puck-title")
             ]
           }
-        ) }),
-        containsZone && zonesForItem.map((subzone) => /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("zones"), children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(LayerTree, { zoneCompound: subzone }) }, subzone))
+        )
+        }, `puck-layer-row-${itemId}`),
+        containsZone ? zonesForItem.map((subzone) => /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassNameLayer("zones"), children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(LayerTree, { zoneCompound: subzone }) }, subzone)) : null
       ]
     }
   );
@@ -8073,24 +8086,22 @@ var LayerTree = ({
     )
   );
   return /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)(import_jsx_runtime36.Fragment, { children: [
-    label && /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("div", { className: getClassName24("zoneTitle"), children: [
-      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassName24("zoneIcon"), children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(Layers, { size: "16" }) }),
+    label ? /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("div", { className: getClassName24("zoneTitle"), children: [
+      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassName24("zoneIcon"), children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(Layers, { size: "16" }) }, "puck-zone-icon"),
       label
-    ] }),
-    /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("ul", { className: getClassName24(), children: [
-      contentIds.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassName24("helper"), children: "No items" }),
-      contentIds.map((itemId, i) => {
-        return /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
-          Layer,
-          {
-            index: i,
-            itemId,
-            zoneCompound
-          },
-          itemId
-        );
-      })
-    ] })
+    ] }, "puck-zone-heading") : null,
+    /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("ul", { className: getClassName24(), children: contentIds.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: getClassName24("helper"), children: "No items" }, "puck-layer-empty") : contentIds.map((itemId, i) => {
+      const layerKey = `${zoneCompound != null ? zoneCompound : "root"}:${i}:${itemId != null && itemId !== "" ? itemId : "missing"}`;
+      return /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
+        Layer,
+        {
+          index: i,
+          itemId,
+          zoneCompound
+        },
+        layerKey
+      );
+    }) }, "puck-zone-ul")
   ] });
 };
 
