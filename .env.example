# Laravel API (existing)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api

# Google Gemini — used only by server route /api/puck-ai (key stays on the server)
GEMINI_API_KEY=
# Optional provider config for AI routes
# AI_PROVIDER=gemini
# GEMINI_MODEL=gemini-2.5-flash
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4.1-mini
